public enum Scenes // values are build indices
{
    LoginScene = 0,
    GameScene = 1
}

public enum StackChangeDirection
{
    Next,
    Prev
}

public enum BlockType
{
    Glass,
    Wood,
    Stone
}